{
  "name": "someday-well-die",
  "version": "1.0.0",
  "private": true,
  "description": "Someday We'll Die — the iOS wrapper around the web app.",
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.5.1",
    "@capacitor/keyboard": "^8.0.5",
    "@capacitor/push-notifications": "^8.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
